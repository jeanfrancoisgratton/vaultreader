#!/usr/bin/env bash

BIN="/opt/bin/vaultreader"
LOG="/var/log/vaultreader.log"

if [ -f "$BIN" ]; then
    chgrp vaultreader "$BIN" || :
    chmod 2755 "$BIN" || :
fi

touch "$LOG"
chown root:vaultreader "$LOG" || :
chmod 0664 "$LOG" || :