#!/usr/bin/env

set -e

BIN="/opt/bin/vaultreader"
LOG="/var/log/vaultreader.log"

if [ -f "${BIN}" ]; then
    chgrp vread "${BIN}" || true
    chmod 2755 "${BIN}" || true
fi

touch "${LOG}"
chown root:vread "${LOG}" || true
chmod 0644 "${LOG}" || true

exit 0
